---
stage: lumbar          # 2 · architecture
status: draft          # draft | sound | building | built
serves: <link to ../sacral_unconscious/... — the desire this gives bones to>
---

# <name of the feature / item>

<!--
This layer prescribes HOW. Be concrete enough to bear weight — but only as concrete as the
thing actually needs. One feature/item per file where possible, so the thoracic layer can
fold it into a wave independently. Delete any section that doesn't want to exist.
-->

## What it is
<The thing itself: feature, component, item. A clear short description.>

## How it's built
<Mechanism: structure, data, interfaces, contracts, boundaries — as much rigor as it needs.>

## Decisions & trade-offs
<Optional. Choices made and what was given up. The reasoning a future reader will want.>

## Open questions
<Optional. What's unresolved. If load-bearing, resolve before folding into a wave.>

---
*Names the desire it serves below; folds into a wave in `../thoracic_infolding/` when sound.*
